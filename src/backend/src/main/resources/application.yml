server:
  port: 8080

spring:
  application:
    name: frame-inventory-backend
  data:
    mongodb:
      uri: mongodb://localhost:27017/frame-inventory

management:
  endpoints:
    web:
      exposure:
        include: health,info
